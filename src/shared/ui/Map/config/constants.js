import {
  CheckIcon,
  CancelIcon,
  BarIcon,
  CinemaIcon,
  RestIcon,
  MusicIcon,
  TheatreIcon,
} from "#shared/ui/Icons/index";

export const iconsPresets = {
  1: BarIcon({ iconColor: "var(--colorRed)" }),
  2: CinemaIcon({ iconColor: "var(--colorRed)" }),
  3: RestIcon({ iconColor: "var(--colorRed)" }),
  4: MusicIcon({ iconColor: "var(--colorRed)" }),
  5: TheatreIcon({ iconColor: "var(--colorRed)" }),
  6: CheckIcon({ iconColor: "var(--colorRed)" }),
  7: CancelIcon({ iconColor: "var(--colorRed)" }),
};
